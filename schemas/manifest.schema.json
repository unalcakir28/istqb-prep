{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://istqb-prep/schemas/manifest.schema.json",
  "title": "Root manifest — certifications, data version",
  "description": "data/manifest.json. The app and the validator read outward from this file (docs/04-data-model.md §2, §7).",
  "type": "object",
  "required": ["dataVersion", "generatedAt", "certifications"],
  "additionalProperties": false,
  "properties": {
    "dataVersion": { "type": "string" },
    "generatedAt": { "type": "string", "format": "date-time" },
    "certifications": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "id", "acronym", "syllabusVersion", "status", "path",
          "languages", "questionCount", "coverage"
        ],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string" },
          "acronym": { "type": "string" },
          "syllabusVersion": { "type": "string" },
          "status": {
            "type": "string",
            "description": "Unverified closed list — docs/04 does not define an enum for this field. Example: 'active'."
          },
          "path": {
            "type": "string",
            "description": "Subdirectory name under data/, e.g. 'ctfl-v4.0.1'."
          },
          "languages": {
            "type": "array",
            "minItems": 1,
            "items": { "enum": ["tr", "en"] },
            "description": "Project rule (CLAUDE.md): every certification includes TR and EN."
          },
          "questionCount": { "type": "integer", "minimum": 0 },
          "coverage": {
            "type": "object",
            "required": ["objectivesTotal", "objectivesCovered", "minPerObjective"],
            "additionalProperties": false,
            "properties": {
              "objectivesTotal": { "type": "integer", "minimum": 0 },
              "objectivesCovered": { "type": "integer", "minimum": 0 },
              "minPerObjective": { "type": "integer", "minimum": 0 }
            }
          }
        }
      }
    }
  }
}
