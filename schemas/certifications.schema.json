{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://istqb-prep/schemas/certifications.schema.json",
  "title": "Exam parameters for all ISTQB certifications (seed)",
  "description": "data/certifications.json. docs/04-data-model.md §2 line 24. This differs from meta.json, which the exam generation engine reads — this is the reference list for the full ISTQB catalog.",
  "type": "object",
  "required": ["source", "certifications"],
  "additionalProperties": false,
  "properties": {
    "source": { "type": "string" },
    "verifiedAt": { "type": "string", "format": "date" },
    "note": { "type": "string" },
    "certifications": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "acronym", "name", "version", "stream", "level",
          "questions", "points", "passPoints", "durationMinutes",
          "extendedDurationMinutes", "ttb"
        ],
        "additionalProperties": false,
        "properties": {
          "acronym": { "type": "string" },
          "name": { "type": "string" },
          "version": { "type": ["string", "null"] },
          "stream": { "enum": ["core", "agile", "specialist"] },
          "level": { "enum": ["foundation", "advanced", "expert", "specialist"] },
          "questions": { "type": "integer", "minimum": 1 },
          "points": { "type": "integer", "minimum": 1 },
          "passPoints": { "type": "integer", "minimum": 1 },
          "durationMinutes": { "type": "integer", "minimum": 1 },
          "extendedDurationMinutes": { "type": "integer", "minimum": 1 },
          "ttb": {
            "type": "object",
            "required": ["offered"],
            "additionalProperties": false,
            "properties": {
              "offered": { "type": "boolean" },
              "languages": {
                "type": "array",
                "items": { "enum": ["tr", "en"] }
              }
            }
          }
        }
      }
    },
    "expertLevel": {
      "type": "object",
      "description": "Expert Level is not in this table; its parameters could not be verified (see docs/03-istqb-reference.md).",
      "properties": {
        "note": { "type": "string" },
        "acronyms": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
